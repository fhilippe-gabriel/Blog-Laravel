/*
 * Projetos Messages
 *
 * This contains all the text for the Projetos page.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.Projetos';

export default defineMessages({
  cadastro: {
    id: `${scope}.cadastro`,
    defaultMessage: 'Cadastro de Projetos'
  }
});
